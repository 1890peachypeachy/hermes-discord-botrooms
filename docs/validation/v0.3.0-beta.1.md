# v0.3.0 beta 1 validation record

This record belongs to the repository commit that contains it. It separates
automated source checks from runtime and real Discord evidence. No Discord IDs,
bot identities, profile names, tokens, or credential paths are included.

## Local automated checks

Run on 2026-08-28 with Python 3.13.14 against the pinned Hermes Agent 0.20.5
fixture at commit `ef46ec03e11452eab74e261147668fb64a3d9fd3` after applying the
repository's guarded compatibility patch:

- Ruff: passed
- Bot Rooms repository suite: 100 passed
- pinned Hermes session-hook regressions: 22 passed, 589 deselected
- compatibility probe: compatible, `generic-session-hooks`
- plugin doctor: manifest `0.3.0b1`, imports and registration passed
- package build: source archive and universal Python wheel built successfully

The downloaded pinned-Hermes fixture matched SHA-256
`d2004e1f6415d85fb1339e5977f0b45f3bcfad1059a345c16d37c99f65318592`.

## Public CI gate

The [CI workflow](../../.github/workflows/ci.yml) repeats lint, the repository
suite, pinned-Hermes regressions, compatibility probing, and plugin validation
on Python 3.11, 3.12, and 3.13. The GitHub check attached to the exact release
commit is the authoritative matrix result and must be green before that commit
is recommended for installation.

## Runtime and Discord acceptance

Maintainer-reported acceptance on 2026-08-28 used the same runtime code as this
candidate in a three-profile macOS deployment. Runtime doctor checks passed,
all three bot identities connected, and the user confirmed the live acceptance
checklist passed, including two top-level messages creating separate threads
with fresh context and thread-local follow-ups.

This live result is an operator report, not an automated artifact. Every new
installation must still run `hermes botrooms doctor --live` and complete the
[live acceptance checklist](../operations.md#live-acceptance-checklist).
